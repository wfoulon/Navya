/** @format */

import {AppRegistry} from 'react-native';
// import App from './App';
import {name as appName} from './app.json';
import PreConfig from './src/Preconfig'

AppRegistry.registerComponent(appName, () => PreConfig);
